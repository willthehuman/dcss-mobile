name: dcss_mobile
description: Native portrait DCSS Webtiles Flutter client.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  web_socket_channel: ^3.0.3
  flame: ^1.20.2
  flutter_secure_storage: ^9.2.4
  dio: ^5.9.0
  path_provider: ^2.1.5
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
